services:
  backend:
    ports:
      - "127.0.0.1:${BACKEND_HOST_PORT:-8100}:8000"

  frontend:
    ports:
      - "127.0.0.1:${FRONTEND_HOST_PORT:-3100}:3000"

  nginx:
    profiles: ["container-edge"]
